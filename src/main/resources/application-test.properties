# Test profile, used by the Spring Boot tests (@ActiveProfiles("test")).
# No datasource URL on purpose: Spring Boot starts an embedded in-memory H2 database with a
# unique name per application context. Flyway creates the schema and DemoDataSeeder adds the demo data.
# The PostgreSQL adapters are covered separately by the Testcontainers integration tests (see README).

# Never call the real Hugging Face API from tests
huggingface.token=
