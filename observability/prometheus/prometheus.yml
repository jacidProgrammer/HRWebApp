# Scrapes the API container of the "full" compose profile on its internal management port (not published).
global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: hr-api
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["api:8081"]
